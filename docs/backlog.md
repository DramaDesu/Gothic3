**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

O**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

A**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

D**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

E**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

G**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

E**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

G**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

B**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

N**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

D**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

G**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

3**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

'**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

P**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

X**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

3**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

0**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

2**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

3**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

J**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

U**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

'**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

0**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

2**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

6**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

A**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

%**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

X**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

%**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

Y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

%**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

Z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

%**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

7**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

P**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

;**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

3**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

0**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

/**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

8**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

M**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

B**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

7**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

5**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

7**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

9**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

5**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

R**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

'**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

B**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

1**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

8**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

0**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

O**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

5**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

3**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

4**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

5**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

6**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

5**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

8**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

7**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

2**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

A**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

6**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

L**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

'**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

_**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

P**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

E**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

W**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

***Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

I**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

F**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

#**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

R**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

T**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

C**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

P**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

U**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

:**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

I**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

`**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

x**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

A**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

,**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

E**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

m**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

R**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

g**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

f**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

p**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

'**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

w**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

k**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

S**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

z**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

c**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

b**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

u**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

-**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

h**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

v**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

d**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

t**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

i**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

o**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

n**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

 **Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

l**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

a**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

y**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

e**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

r**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

s**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

.**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.


**Step-up works and is barely tested.** Withdrawing the claim that stood here:
the world does place stairs, and the search that said otherwise was simply a bad
one - it looked at the visual mesh names a sector lists, and found them once it
looked at the collision file each placement names instead. There are stairs at
`59403 7899 58444` and half a dozen other places in the streamed rectangle.

Walking at one of them took a step for the first time, and the step lifted the
body by 97 against a threshold of 70: raising the capsule and letting it settle
can leave it standing well above what the threshold allows, so a climb passed as
a stride. Clamped now. What is still missing is a clean approach - a body
dropped near a staircase spends the run falling rather than walking, so the
positive case rests on that single observed step.

